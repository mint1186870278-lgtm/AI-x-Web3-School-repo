# Week 2｜方向研究｜AI × Web3 问题地图与主方向选择

> 课程：AI × Web3 School｜Week 2 Module A  
> 完成日期：2026-05-25  
> 主方向：**Payment / Commerce / Settlement**

---

## 一、AI × Web3 问题地图（6 个方向）

| 方向 | 核心问题 | AI 的角色 | Web3 的机制 |
|------|---------|----------|------------|
| **① Payment / Commerce / Settlement** | Agent 如何在无人工干预的情况下完成报价、支付、结算，同时保证资金安全和可审计？ | 理解用户意图、生成报价草稿、监控结算状态、识别异常并触发人工确认 | x402 paywall、ERC-20 转账、智能合约托管（Escrow）、链上收据、ERC-8183 结算标准 |
| **② Identity / Reputation / Capability** | 一个 Agent 如何向另一个 Agent 或用户证明「我是谁、我能做什么、我值得信任」？ | 生成结构化能力声明（Agent Profile）、解析对方 profile、评估历史声誉得分 | DID（去中心化身份）、ERC-8004 Agent 注册标准、链上声誉记录、可验证凭证（VC） |
| **③ Wallet / Permission / Safe Execution** | Agent 发起链上操作时，如何保证它只能做被授权的事，超权操作被拦截？ | 动态风险评估、解释交易意图、自动拒绝规则外操作、向用户展示草稿 | ERC-4337 账户抽象、Session Key、Safe Multisig、Guard/Policy 链上策略、`revokeSessionKey()` |
| **④ Privacy / Security / Sovereignty** | Agent 处理敏感数据或高风险操作时，如何防止数据泄露、模型被操控、用户主权被侵蚀？ | Prompt Injection 检测、威胁建模、隐私推理（在 TEE 内运行）、攻击面分析 | ZK Proof（零知识证明）、可信执行环境（TEE）、链上访问控制、数据主权合约 |
| **⑤ Dev Tooling / Agent Workflow** | 开发者如何高效构建、测试、部署 AI × Web3 应用？Agent 工作流如何与链上工具对接？ | 代码生成与审计辅助、工作流编排、链感知上下文注入、ABI 解析与调用生成 | Hardhat/Foundry 测试框架、ABI/Event 标准、The Graph 索引、MCP 工具调用协议 |
| **⑥ Governance / Coordination / Public Goods** | DAO 如何借助 AI 提升治理效率，同时保证关键决策仍由人或治理流程确认？ | Proposal 摘要、投票趋势预测、贡献追踪、会议纪要转行动项 | DAO 投票合约（Governor）、Token 权重投票、Timelock、链上预算执行、多签确认 |

---

## 二、为什么不是纯 AI / 纯 Web3 问题？

### 分析方向 ①：Payment / Commerce / Settlement

**为什么不是纯 AI 问题？**

AI 能理解"帮我付款给供应商"的意图，能生成报价、选择支付路径、计算汇率——但它无法独立完成以下环节：
- **资金托管**：AI 没有"钱"，也无法强制锁定资金等待交付验收。没有智能合约 Escrow，预付款就是单方面信任。
- **不可篡改的收据**：AI 生成的付款确认可以被伪造或否认；链上交易哈希是唯一不可抵赖的证据。
- **跨主体结算**：多个 Agent、多个服务方之间的资金分配需要可信第三方或链上规则，AI 自己无法扮演可信仲裁者。

**为什么不是纯 Web3 问题？**

智能合约能托管资金、自动结算、提供不可篡改记录——但它无法独立完成：
- **意图理解**：合约只处理结构化的函数调用，无法理解"帮我找最便宜的方案付给供应商"这类自然语言需求。
- **动态谈判**：报价、条款协商、交付物定义需要理解上下文、匹配语义，这是 LLM 的能力范围。
- **异常处理**：交付失败、服务质量争议、仲裁需要语言理解和推理，合约只能处理预先编码的分支。

**结论**：Payment 场景需要 AI 做意图层、谈判层、异常判断，Web3 做执行层、结算层、审计层。两者缺一不可。

---

### 分析方向 ③：Wallet / Permission / Safe Execution

**为什么不是纯 AI 问题？**

AI 能评估"这笔操作是否合理"、能识别 Prompt Injection 企图——但：
- **执行层无法被 AI 自己约束**：如果 AI 判断"这笔交易有风险"，它无法阻止自己（或另一个 AI 进程）实际发出交易。约束必须在执行层（链上策略）硬编码。
- **权限边界需要密码学保证**：白名单、金额上限必须写进合约逻辑，不能只靠 AI 的"我会遵守规则"承诺。
- **撤销机制需要链上状态**：`revokeSessionKey()` 是链上操作，AI 无法在链下"吊销"一个密钥。

**为什么不是纯 Web3 问题？**

合约能强制执行白名单和金额上限——但：
- **风险动态评估**：合约只能处理静态规则（金额 > 500 USDC → 拒绝），无法理解"这个操作在当前市场环境下是否异常"。
- **意图模糊处理**：用户说"帮我赚最多的收益"，合约无法把模糊意图翻译成具体调用参数。
- **用户体验**：纯链上方案的交互是十六进制 calldata，需要 AI 层做自然语言桥接。

---

## 三、主方向选择：Payment / Commerce / Settlement

**选定方向 ①：Payment / Commerce / Settlement**

### 选择理由

1. **最具体的 AI × Web3 交叉点**：意图理解（AI）+ 链上结算（Web3）的分工最清晰，每一步都能写出具体的系统设计。
2. **有真实标准可对标**：x402、ERC-8183、MPP、ERC-8004 都有公开草案，不是纸上谈兵。
3. **最小 demo 可落地**：一个"Agent 请求服务 → x402 付款 → 获取结果"的闭环可以用现有工具实现（或伪代码描述）。
4. **覆盖本周最高分任务**：进阶实践 x402 + CAW（40 分）直接在这个方向上展开。
5. **与 Week 1 基础衔接**：Machine Payment（Day 5 已读）、Agent Wallet（Day 3 已读）、智能合约（已部署 Hello.sol）是直接前置知识。

### 本周主线场景

> **「一个 AI Agent 作为服务消费方，向提供受 x402 保护的 API 服务方发起请求，识别付款要求，通过 CAW/Pact 在预算范围内完成支付，获取服务结果，并留下可审计的完整记录。」**

### 暂不选择的方向（Backlog）

| 方向 | 暂不选择的原因 |
|------|--------------|
| ② Identity / Reputation | 标准（ERC-8004）仍处于草案阶段，真实可验证的链上身份系统尚不成熟；更适合作为 Payment 方向的配套（「谁在付款」的身份层），而非独立主线 |
| ④ Privacy / Security | ZK Proof 技术门槛较高（需要 Circom/Noir 等专业工具），在 ≤1h/天的时间约束下难以产出可运行 demo；安全设计会作为 Payment 方向的风险层覆盖 |
| ⑤ Dev Tooling | 偏基础设施，产出更多是工具评测而非系统设计；适合在其他方向 demo 实现过程中自然覆盖 |
| ⑥ Governance | 政治/协作逻辑强，技术抓手弱；链上投票合约的 AI 集成目前案例少，验证标准模糊 |

---

## 四、统一判断框架：主方向（Payment）深度验证

> 官方统一框架——每个方向用同一组问题检验，避免只把 AI 和 Web3 两个词放在标题里。

**Q1：这个问题如果没有 AI，是否仍然成立？AI 承担什么能力？**

没有 AI，机器支付退化为硬编码脚本：只能处理固定格式的请求，无法理解"找最便宜的推理服务"、无法在付款前判断交付物是否合理、无法识别 Prompt Injection 企图篡改支付目标。AI 在此承担的是**意图解析、动态决策、异常识别**三种能力——这三种能力依赖语言理解，脚本无法替代。

**Q2：这个问题如果没有 Web3，是否仍然成立？Web3 提供什么机制？**

没有 Web3，可以用传统 API + 信用卡实现 agent 付款，但会丢失：① 无需可信第三方的 trustless 结算；② 不可篡改的付款收据（对仲裁至关重要）；③ Escrow 合约对"先付款、后交付"风险的对冲；④ 跨主体的原子化结算。Web3 提供的不是"更快的支付"，而是**可验证的执行记录和去信任的资金流转**。

**Q3：谁发起、谁执行、谁验收、谁付款、谁承担失败成本？**

| 角色 | 主体 |
|------|------|
| 发起方 | 人类用户（下达高级指令）或上游 Agent |
| 执行方 | 消费方 Agent（解析意图、构建请求、触发支付） |
| 服务方 | API/推理节点（执行任务、返回结果） |
| 验收方 | Agent 自动验收（结果符合预期格式/内容），超出自动范围则回退人工 |
| 付款方 | Agent 钱包（CAW/Pact 限定预算） |
| 仲裁方 | 链上 Escrow 合约 + 人工（争议时） |
| 失败成本承担方 | 视 Escrow 条款：交付失败 → 退款给消费方；恶意攻击 → 预算上限兜底 |

**Q4：哪些动作可以自动化，哪些必须人工确认？**

| 动作 | 自动化 | 人工确认 |
|------|--------|---------|
| 读取链上状态、报价 | ✅ | — |
| 小额支付（< 阈值，已知服务方） | ✅ | — |
| 大额支付（> 阈值） | — | ✅ |
| 首次接入新服务方 | — | ✅ |
| 任何 `approve` 操作 | — | ✅ |
| 触发 Escrow 退款 / 争议 | — | ✅ |
| 生成收据、写审计日志 | ✅ | — |

**Q5：结果如何验证？验证成本是否低于人工协调成本？**

- 链上交易哈希是支付的唯一可信证据，验证成本接近零（Etherscan 公开可查）
- 服务结果验证取决于交付物类型：结构化输出（JSON、合约地址）可自动断言；主观内容需要人工或第三方 Evaluator
- 总体验证成本 << 人工协调成本，这正是该方向成立的核心经济理由

**Q6：它更像什么类型的问题？**

**协议 / 标准层**（x402、MPP、ERC-8183）+ **权限系统**（CAW/Pact）+ **应用层体验**（agent 自主支付闭环）。三层叠加，不是纯产品也不是纯研究。

**Q7：如果失败，最可能失败在哪里？**

| 失败原因 | 概率 | 缓解方式 |
|---------|------|---------|
| 接口不成熟（x402 生态早期） | 高 | 用伪代码 + 架构图验证逻辑，不依赖完整生态 |
| 信任不可建立（服务方跑路） | 中 | Escrow 合约对冲；链上声誉记录 |
| 成本过高（gas + 延迟） | 中 | L2 部署；链下报价 + 链上结算分离 |
| 用户不愿改变流程 | 低 | agent 自动化隐藏复杂性，用户感知是"AI 帮我搞定了" |

---

## 五、方向判断矩阵（Payment / Commerce / Settlement）

| 维度 | 判断 |
|------|------|
| **结构性需求** | ✅ 长期存在：机器对机器支付随 AI agent 经济扩张而增长，不依赖单一热点项目 |
| **验证可能性** | ✅ 可验证：流程图 + x402 伪代码 + 链上交易记录可完整展示逻辑 |
| **最小切入点** | ✅ 一周内可做：问题拆解 + 流程图 + mock x402 响应处理脚本 |
| **风险边界** | ⚠️ 涉及链上资金：需明确预算上限、Escrow 条款、签名不进仓库 |
| **后续承接** | ✅ 自然进入：Week 3 proposal → Cobo CAW Hackathon track → x402 reference implementation |

---

## 六、主方向核心问题拆解

> 详细展开见 `week2-proposal.md`

| 维度 | 内容 |
|------|------|
| **参与方** | 服务消费方 Agent、服务提供方（API/AI 推理节点）、预算管理层（CAW/Pact）、链上结算合约、人类用户（审批高风险操作） |
| **核心流程** | 请求 → 402 响应 → 识别付款要求 → 预算检查 → 支付意图 → 链上转账 → 服务交付 → 收据存证 |
| **AI 作用** | 解析 402 响应头、匹配服务价格与预算、异常识别、触发人工确认 |
| **Web3 机制** | ERC-20 转账、x402 收款验证、链上 Escrow、CAW 预算策略、交易哈希作为收据 |
| **自动化边界** | 小额（< 阈值）可自动执行；大额、新服务方、频率异常须人工确认 |
| **人工确认点** | 大额支付、首次新服务方、任何 approve、触发争议 |
| **验证方式** | 链上交易哈希（Etherscan）+ 审计日志（意图→草稿→执行→收据）|
| **主要风险** | 服务方伪造 402 响应、付款后不交付、Prompt Injection 操控支付路径、gas 波动导致预算穿透 |
